<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/listItemContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/categoryIconIv"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="top"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="32dp"
        android:layout_marginTop="24dp"
        tools:src="@drawable/category_icon_personal" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="17dp"
        android:layout_marginBottom="14dp"
        android:layout_gravity="top"
        android:layout_weight="1"
        android:orientation="vertical">

        <com.zoomlee.zoo.ui.view.ZMTextView
            android:id="@+id/alertNameTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="PERSONAL"
            android:textColor="@color/text_empty"
            android:textSize="@dimen/text_normal" />

        <com.zoomlee.zoo.ui.view.ZMTextView
            android:id="@+id/alertValueTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:singleLine="true"
            tools:text="SSN Card, Passport,SSN Card, Passport,SSN Card, Passport,SSN Card, Passport,SSN Card, Passport"
            android:textColor="@color/text_empty"
            android:textSize="@dimen/text_normal" />
    </LinearLayout>

    <com.zoomlee.zoo.ui.view.ZMTextView
        android:id="@+id/alertsTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        app:font="@string/font_light"
        tools:text="today"
        android:textColor="@color/text_empty"
        android:textSize="@dimen/text_small" />
</LinearLayout>